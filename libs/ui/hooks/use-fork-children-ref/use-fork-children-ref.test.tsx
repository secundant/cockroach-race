import { useForkChildrenRef } from './use-fork-children-ref';

describe.skip('hooks/use-fork-children-ref', () => {
  // TODO
  test('Should pass', () => {
    expect(useForkChildrenRef({} as any, {} as any)).toBe(true);
  });
});
