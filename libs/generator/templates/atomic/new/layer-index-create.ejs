---
to: <%= h.path.resolve(rootDir, layer, 'index.ts') %>
unless_exists: true
---
// atomic - autogenerated "<%= layer %>" layer

// atomic:re-export:<%= name %>
// atomic:re-export:<%= name %>:component

export { default as <%= h.changeCase.pascal(name) %> } from './<%= name %>';

// atomic:re-export:<%= name %>:types

export type { <%= h.changeCase.pascal(name) %>Props } from './<%= name %>';
