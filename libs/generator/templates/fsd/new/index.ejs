---
to: <%= h.path.resolve(baseDir, 'index.ts') %>
---
// This file is generated by "hygen fsd new"
// Manual changes might be lost - proceed with caution!
// fsd:<%= name %>

import * as <%= h.changeCase.pascal(name) %>Model from './model';
import { <%= h.changeCase.pascal(name) %>View } from './ui';
<% if (parts.includes('types')) { %>
import type { <%= h.changeCase.pascal(name) %>, <%= h.changeCase.pascal(name) %>Id } from './types';
<% } %>

// fsd:<%= name %>:public-api
export {
    // fsd:<%= name %>:public-api:ui
    <%= h.changeCase.pascal(name) %>View,

    // fsd:<%= name %>:public-api:model
    <%= h.changeCase.pascal(name) %>Model,
<% if (parts.includes('types')) { %>

    // fsd:<%= name %>:public-api:types
    <%= h.changeCase.pascal(name) %>, <%= h.changeCase.pascal(name) %>Id,
<% } %>
}
